#include <stdio.h>
int main()
{
    int eggPrice = 10;
    float noodlesPrice = 15.2787;
    int myAge;
    // printf("How old are you?");
    // scanf("%d", &myAge);

    int a, b;
    char p;

    scanf("%d%c", &a, &p);
    printf("%d%c", a, p);

    // printf("The price is %d and noodles price is %0.2f. I am %d years old", eggPrice, noodlesPrice, myAge);
    return 0;
}